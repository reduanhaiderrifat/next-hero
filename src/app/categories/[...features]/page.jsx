import React from 'react';

const page = ({params}) => {
    console.log(params);
    return (
        <div className='h-screen'>
            this is features page 
        </div>
    );
};

export default page;